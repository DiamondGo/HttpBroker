server:
  listen: ":8080"
  tls:
    enabled: false
    cert_file: ""
    key_file: ""

tunnel:
  poll_timeout: "45s"
  session_timeout: "15m"

auth:
  enabled: false
  token: ""  # Set a shared secret token when auth is enabled (e.g., "my-secret-token-123")

logging:
  level: "info"
