server:
  listen: ":9090"
  tls:
    enabled: false
    cert_file: ""
    key_file: ""

tunnel:
  poll_timeout: "45s"
  session_timeout: "60m"

auth:
  enabled: false
  token: ""  # Set a shared secret token when auth is enabled (e.g., "my-secret-token-123")

logging:
  level: "debug"

